static int gcd(int a,int b)
	{
		return b!=0 ? gcd(b,a%b) : a;
	}

for (int i = 1; i <= 100; ++i)
                if ((n*i) % 100 == 0) {
                    System.out.println(i);
                    break;
                }


frquency count;

for(int i=0;i<a.length;i++)
{
  int idx=frq[i]=a[i]-min;
  frq[idx]++;
}

2D:
int y=arr[i][length-j];// reverser[2d]
